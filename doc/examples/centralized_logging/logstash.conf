input {
  gelf {
    port => 12201
  }
  file {
    path => "/path/to/another/log.log"
    codec => json
    start_position => "beginning"
    type => "syslog"
    sincedb_path => "/usr/share/logstash/sincedb"
  }
}

filter {
  if [tag] == "harmony-ap" {
    grok {
      match => {
        "message" => "^%{TIMESTAMP_ISO8601:timestamp}%{SPACE}%{LOGLEVEL:log_level} \[%{DATA:d_user}\] \[%{DATA:d_domain}\] \[%{DATA:d_messageId}\] \[%{DATA:thread}\] %{DATA:logger_class}:%{NUMBER:line_number} - %{GREEDYDATA:log_message}$"
      }
      break_on_match => false
    }
    grok {
      match => {
        "message" => "^%{TIMESTAMP_ISO8601:timestamp} %{LOGLEVEL:log_level} \[%{DATA:thread}\] %{JAVAFILE:logger_class} %{GREEDYDATA:log_message}$"
      }
    }
  } else if [type] == "syslog" {
    grok {
      match => {
        "log" => "%{SYSLOGTIMESTAMP:timestamp} %{SYSLOGHOST:hostname} %{DATA:program}(?:\[%{POSINT:pid}\])?: %{GREEDYDATA:log_message}"
      }
    }
  }
}

output {
  elasticsearch {
    hosts => ["https://elasticsearch-node:9200"]
    user => "${ELASTICSEARCH_USERNAME}"
    password => "${ELASTICSEARCH_PASSWORD}"
    ssl_enabled => true
    ssl_certificate_authorities => "/usr/share/logstash/config/certs/ca/ca.crt"
  }
}
