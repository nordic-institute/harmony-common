$TTL	60
@	IN	SOA	ns. root.neds. (
			      2		; Serial
			 604800		; Refresh
			  86400		; Retry
			2419200		; Expire
			 604800 )	; Negative Cache TTL
;
@	IN	NS	ns.
ns	IN	A	{{ ansible_default_ipv4.address }}
